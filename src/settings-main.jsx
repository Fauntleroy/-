import React from 'react';
const ReactDOM = require('react-dom');

import Settings from './components/settings';

const appElement = document.querySelector('#app');

ReactDOM.render(<Settings />, appElement);
